/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["cristianosilvadev.com",
    "lh3.googleusercontent.com"
  ],
  }
}

module.exports = nextConfig
